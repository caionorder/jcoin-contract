{
  "name": "jcoin-contract",
  "version": "1.0.0",
  "description": "JOINADS COIN (JCOIN) BEP-20 Token Smart Contract",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bscMainnet",
    "clean": "hardhat clean"
  },
  "keywords": ["bep20", "bsc", "token", "solidity", "hardhat"],
  "author": "JoinAds",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0"
  }
}
